package com.example.peer.schedule.repository;

import com.example.peer.schedule.entity.ScheduleCreateRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScheduleCreateRuleRepository extends JpaRepository<ScheduleCreateRule, Long> {
}
